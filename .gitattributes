# Enforce LF line endings for all text files
* text=auto eol=lf

# Shell scripts must always use LF
*.sh text eol=lf
switchboot-cli text eol=lf

# Windows-specific files can use CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf
